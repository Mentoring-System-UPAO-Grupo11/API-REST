package com.hampcode.tutoria_apoyo_academico.service.impl;

public class ReporteServiceImpl /*implements ReporteService*/ {

    /*List<Reporte> reportes = new ArrayList<Reporte>();

    @Override
    public Reporte createReporte(Reporte reporte) {
        Reporte newReporte = Reporte.builder()
                .id(1L)
                .fecha(reporte.getFecha())
                .reporteStatus(reporte.getReporteStatus())
                .tutor(reporte.getTutor())
                .calificacion(reporte.getCalificacion())
                .asistencia(reporte.getAsistencia())
                .recomendaciones(reporte.getRecomendaciones())
                .build();
        reportes.add(newReporte);

        return newReporte;
    }

    @Override
    public List<Reporte> getAllReportes() {
        return null;
    }*/
}
